PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/gradesense

# LLM provider — real API key goes in your own .env (never committed)
GROQ_API_KEY=
LLM_PROVIDER=mock
# LLM_PROVIDER=groq   # switch to this once you add a real key
